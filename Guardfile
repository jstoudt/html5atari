# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{.+\.html})
  watch(%r{css/.+\.css})
  watch(%r{js/.+\.js})
  watch(%r{images/.+\.(gif|jpg|jpeg|png)})
end
